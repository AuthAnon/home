import {} from './App';

it('renders learn react link', () => {
  // const { getByText } = render(<App />);
  // const linkElement = getByText(/AuthAnon/i);
  // expect(linkElement).toBeInTheDocument();
});
